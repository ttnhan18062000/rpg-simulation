syntax = "proto3";

package notifier;

service Notifier {
  rpc notifyUpdate (updateRequest) returns (updateResponse);
}

message updateRequest {
  string message = 1;
}

message updateResponse {
  bool success = 1;
}